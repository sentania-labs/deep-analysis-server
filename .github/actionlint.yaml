# Custom runner labels actionlint cannot know about.
#   lab     the ARC scale set on the homelab Kubernetes cluster
#           (sentania-labs/homelab-runner). Matched by exact name.
self-hosted-runner:
  labels:
    - lab
