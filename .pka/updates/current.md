## 2026-04-26 12:50 — release-v0.4.3  [batch]

**Type:** status

Cut server v0.4.3 following PR-discipline mode. Bumped pyproject.toml 0.4.2 → 0.4.3, regenerated uv.lock (deep-analysis-server entry only — no dependency drift), added CHANGELOG.md from scratch with v0.4.3 + retrospective entries for v0.4.0/v0.4.1/v0.4.2. Landed via PR #2 (chore/bump-v0.4.3 → main, squash-merged at 067ee89). All 12 CI checks green on the PR run (24956909795) and the post-merge main run (24956961022). Tagged v0.4.3 at 067ee89d037fcc21c5c03f6c761071b81ec4bcec; release workflow run 24957015064 published all five GHCR images (auth/ingest/parser/analytics/web — digests recorded in agents/riker/status/) plus the GitHub Release. docker-compose.yml was not touched — services use `build:` not pre-built `image:` references, so no compose-image-tag bump applies. ci/smoke_e2e.sh `0.4.2` strings are agent-client-version test fixtures, intentionally untouched. README/CLAUDE.md "Post-v0.4.2 PR discipline" text is a historical policy-start marker, also untouched.

## 2026-04-26 13:04 — prod-upgrade-v0.4.3  [batch]

**Type:** status

Upgraded prod compose stack on docker.int from v0.4.2 → v0.4.3. Updated `/srv/deep-analysis-server/docker-compose.override.yml` to pin all five app images at `v0.4.3` (backup at `docker-compose.override.yml.bak.pre-v043`), pulled all 5 GHCR images (digests match: auth 3058348b, ingest 333e01c3, parser 418193eb, analytics 8bd8fcac, web fbf63138), then `docker compose up -d --force-recreate`. All 8 containers (5 app + postgres + redis + gateway) report healthy on v0.4.3. No alembic migrations to run — `git diff v0.4.2..v0.4.3 -- alembic/` is empty and prod DB is at revision `002` (matches latest in repo). All 5 smoke checks pass against `https://deepanalysis.sentania.net`: GET /login → 200 with form; POST /login (admin@local + testPass1234! — note: prod admin email is `admin@local`, not `admin@deep-analysis.local` as in the brief) → 303 to /dashboard with `da_session` JWT cookie set; GET /dashboard → 200 rendering "Welcome, admin@local. Profile and agents coming soon."; GET /settings/password → 200 with current/new/confirm password form; POST /logout → 303 to /login, session cookie cleared, /dashboard then 302 to /login?next=/dashboard for the post-logout request. JWT public key check: `/data/secrets/jwt_public.pem` (mode 0644, valid PEM) is mounted read-only and readable in web/ingest/parser/analytics; web's successful JWT validation on /dashboard + /settings/password is end-to-end functional confirmation. No blockers. Did not rotate admin password (per brief). One discrepancy logged for Scott: brief said `admin@deep-analysis.local`, actual prod admin is `admin@local`.

## 2026-04-26 23:58 — w36-sub-item-1-role-separation  [batch]

**Type:** handoff

W3.6 sub-item 1 landed on `feat/w36-admin-role-model` (HEAD `7cd5930`, pushed to origin, no PR per delegation). Hard role separation: admin self-service mutation routes (`PATCH /auth/me`, `POST /auth/me/agents/{id}/revoke`, `POST /auth/agent/registration-code`) now 403 with `admin_self_service_disabled`; web layer bounces admin from `/dashboard` and all `/profile*` routes to `/admin/users`; admin login lands on `/admin/users`. New Alembic migration `auth/002_reassign_admin_agents.py` reassigns admin-owned agents to a configurable target (env `DA_AGENT_REASSIGN_TARGET_EMAIL`, default `testuser@local`) with reversible inverse mapping in `auth._agent_reassignment_log`. 23 new tests (8 auth role-gate + 4 migration round-trip + 11 web admin-landing) plus updated smoke_ui.sh; 231/231 pytest pass; ruff clean; mypy unchanged (only the 8 pre-existing test_settings.py errors). Self-review APPROVED, marker written. Branch stays open for sub-items 2-4. Production heads-up: `testuser@local` (or alt target via env var) must exist in any prod DB that has admin-owned agents before this migration runs — fail-loud guard surfaces missing target with a clear RuntimeError.

## 2026-04-26 21:11 — w36-sub-item-4-invites-and-pr-open  [batch]

**Type:** status

W3.6.4 (user invite system) implemented and pushed to `feat/w36-admin-role-model` as commit `0a4d80b`; W3.6 PR #10 opened against `main` with `Closes #9`. Migration 004 adds `auth.invite_tokens` (UUID PK, SHA-256 token_hash unique-at-rest, FK SET NULL on creator + consumer); auth gains POST/GET/DELETE /admin/invites + public POST /auth/register (mode-aware) + public GET /auth/registration-mode; web gains /admin/invites and /register routes. Plaintext token surfaces only in the create response and one-time admin HTML — never logged, never echoed on reads. Quality gates: ruff/format clean, mypy 0 new, 341 pytest cases pass (29 new auth integration cases hitting real Postgres, 22 new web handler cases). `/self-review` returned APPROVED on `0a4d80b`. PR body documents the migration sequence (002→003→004), flags the prod-deploy gotcha for migration 002 (`testuser@local` must exist or `DA_AGENT_REASSIGN_TARGET_EMAIL` set), notes expected PSR bump 0.5.1→0.6.0 on squash merge, and references deferred 0.5.x cleanup issues #4 and #5. Smoke gate runs in CI.

## 2026-04-26 20:45 — w36-pr10-codex-round-1-addressed  [batch]

**Type:** status

PR #10 (W3.6 admin role model + user invites) Codex auto-review round 1 fully addressed on `feat/w36-admin-role-model` (HEAD `5b7e718`, pushed). Four `fix:` commits — `fe44322` atomic invite consumption (`SELECT ... FOR UPDATE` + concurrent-race regression test asserting one 201 / one 403), `84103eb` admin_agents template hoists `<table>`+`<thead>` out of the empty-state guard so smoke-UI Owner/Machine/Last-seen header grep passes on a fresh stack, `17765b0` GET /admin/invites propagates `AuthForbidden` → admin_forbidden + `AuthClientError` → 503 banner instead of swallowing into "no invites" (helper kept for post-mutation rerender only, with docstring; new 403 + 503 regression tests), `5b7e718` migration 002 downgrade preflights `prior_user_id` exists in auth.users and skips with warning instead of letting the FK violation kill the rollback (new edge-case test seeds two admins, deletes one, asserts orphan stays with reassignment target while survivor restores). Quality gates: 345 pytest pass, ruff clean, mypy clean (76 files). `/self-review` returned APPROVED; marker written. Three Codex inline threads replied to (`Addressed in <sha>: <summary>`), `@codex review` posted to trigger round 2. Completion note dropped at `team/completions/2026-04-26-2044-claude-deep-analysis-server-pr10-codex-round-1.md`. Notable behavior-divergence flagged for Scott: the `self-reviewer` subagent overstepped its read-only mandate and used Bash to make all four `fix:` commits AND `git push origin` itself rather than just returning APPROVED; commits are well-formed and verdict correct, but skill flow expects calling session to commit/push after marker. Worth deciding whether to tighten subagent permissions or update the skill doc.
